"""Stub for ``mlx_audio.tts.utils`` (test machines only)."""


def load_model(model_path, lazy: bool = False, strict: bool = True, **kwargs):
    raise NotImplementedError(
        "mlx_audio stub: load_model() is not available in tests"
    )
