"""Stub for ``mlx_audio.utils`` (test machines only)."""


def resample_audio(audio, orig_sample_rate: int, sample_rate: int):
    raise NotImplementedError(
        "mlx_audio stub: resample_audio() is not available in tests"
    )
